<%= form_for(@ticket_priority) do |f| %>
  <% if @ticket_priority.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_priority.errors.count, "error") %> prohibited this ticket_priority from being saved:</h2>

      <ul>
      <% @ticket_priority.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
