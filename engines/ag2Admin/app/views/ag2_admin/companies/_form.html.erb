<script>
  jQuery(function($) {
    // when the #company_town_id field changes
    $("#company_town_id").change(function() {
      // make a POST call and replace the content
      var company = $('select#company_town_id :selected').val();
      if (company == "")
        town = "0";
      jQuery.getJSON('update_province_textfield/' + company, function(data) {
        $("#company_province_id").val(data.id);
      })
      return false;
    });
  })
</script>

<%= simple_form_for @company do |f| %>
<%= f.error_notification %>
<%= display_base_errors resource %>
<%= f.input :name, :required => true, :autofocus => true %>
<%= f.input :fiscal_id, :required => true %>
<%= f.association :street_type %>
<%= f.input :street_name %>
<%= f.input :street_number %>
<%= f.input :building %>
<%= f.input :floor %>
<%= f.input :floor_office %>
<%= f.association :zipcode %>
<%= f.association :town %>
<%= f.input :province_id %>
<%= f.button :submit, :class => 'btn-primary' %>
<% end %>
