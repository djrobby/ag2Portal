<%= form_for(@currency_instrument) do |f| %>
  <% if @currency_instrument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@currency_instrument.errors.count, "error") %> prohibited this currency_instrument from being saved:</h2>

      <ul>
      <% @currency_instrument.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
