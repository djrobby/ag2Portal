<%= form_for(@bank_account_class) do |f| %>
  <% if @bank_account_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bank_account_class.errors.count, "error") %> prohibited this bank_account_class from being saved:</h2>

      <ul>
      <% @bank_account_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
