<% prices.each do |w| %>
<tr>
  <td><%= w.supplier.supplier_code %></td>
  <td><%= w.supplier.name %></td>
  <td><%= w.code unless w.code.blank? %></td>
  <td align="right"><%= number_to_currency(w.price, precision: 4, locale: :es) unless w.price.blank? %></td>
</tr>
<% end %>
