<h1>Listing inventory_count_types</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @inventory_count_types.each do |inventory_count_type| %>
  <tr>
    <td><%= link_to 'Show', inventory_count_type %></td>
    <td><%= link_to 'Edit', edit_inventory_count_type_path(inventory_count_type) %></td>
    <td><%= link_to 'Destroy', inventory_count_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Inventory count type', new_inventory_count_type_path %>
