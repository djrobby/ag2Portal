<%= form_for(@inventory_count_type) do |f| %>
  <% if @inventory_count_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_count_type.errors.count, "error") %> prohibited this inventory_count_type from being saved:</h2>

      <ul>
      <% @inventory_count_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
