<%= form_for(@purchase_price) do |f| %>
  <% if @purchase_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_price.errors.count, "error") %> prohibited this purchase_price from being saved:</h2>

      <ul>
      <% @purchase_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
