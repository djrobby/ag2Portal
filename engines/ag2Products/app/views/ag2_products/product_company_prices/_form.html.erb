<%= form_for(@product_company_price) do |f| %>
  <% if @product_company_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_company_price.errors.count, "error") %> prohibited this product_company_price from being saved:</h2>

      <ul>
      <% @product_company_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
