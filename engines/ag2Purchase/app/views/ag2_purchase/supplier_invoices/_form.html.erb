<%= form_for(@supplier_invoice) do |f| %>
  <% if @supplier_invoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplier_invoice.errors.count, "error") %> prohibited this supplier_invoice from being saved:</h2>

      <ul>
      <% @supplier_invoice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
