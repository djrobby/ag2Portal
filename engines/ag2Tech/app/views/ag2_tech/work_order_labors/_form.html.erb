<%= form_for(@work_order_labor) do |f| %>
  <% if @work_order_labor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_order_labor.errors.count, "error") %> prohibited this work_order_labor from being saved:</h2>

      <ul>
      <% @work_order_labor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
