<%= form_for(@ratio_group) do |f| %>
  <% if @ratio_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ratio_group.errors.count, "error") %> prohibited this ratio_group from being saved:</h2>

      <ul>
      <% @ratio_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
