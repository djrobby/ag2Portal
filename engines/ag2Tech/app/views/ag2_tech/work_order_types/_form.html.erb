<%= form_for(@work_order_type) do |f| %>
  <% if @work_order_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_order_type.errors.count, "error") %> prohibited this work_order_type from being saved:</h2>

      <ul>
      <% @work_order_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
