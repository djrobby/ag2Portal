<%= form_for(@charge_account) do |f| %>
  <% if @charge_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@charge_account.errors.count, "error") %> prohibited this charge_account from being saved:</h2>

      <ul>
      <% @charge_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
