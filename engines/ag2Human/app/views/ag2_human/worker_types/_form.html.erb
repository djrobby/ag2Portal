<%= form_for(@worker_type) do |f| %>
  <% if @worker_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@worker_type.errors.count, "error") %> prohibited this worker_type from being saved:</h2>

      <ul>
      <% @worker_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
