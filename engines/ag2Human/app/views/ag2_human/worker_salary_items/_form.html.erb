<%= form_for(@worker_salary_item) do |f| %>
  <% if @worker_salary_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@worker_salary_item.errors.count, "error") %> prohibited this worker_salary_item from being saved:</h2>

      <ul>
      <% @worker_salary_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
