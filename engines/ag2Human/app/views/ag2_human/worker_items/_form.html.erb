<%= form_for(@worker_item) do |f| %>
  <% if @worker_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@worker_item.errors.count, "error") %> prohibited this worker_item from being saved:</h2>

      <ul>
      <% @worker_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
