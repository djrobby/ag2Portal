<%= form_for(@collective_agreement) do |f| %>
  <% if @collective_agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collective_agreement.errors.count, "error") %> prohibited this collective_agreement from being saved:</h2>

      <ul>
      <% @collective_agreement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
