<script>
  $(document).ready(function() {
    $("#div_table th a, #div_table #pages a").on("click", function() {
      $.getScript(this.href);
      return false;
    });
  }); 
</script>

<table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th><%= sortable "id", t('activerecord.attributes.service_point_location.id') %></th>
      <th><%= sortable "name", t('activerecord.attributes.service_point_location.name') %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @service_point_locations.each do |service_point_location| %>
      <tr>
        <td><%= service_point_location.try(:id) %></td>
        <td><%= link_to service_point_location.try(:name), service_point_location_path(service_point_location) %></td>
        <td> <%= render :partial => '/layouts/crud/edit', :locals => { :model => ServicePointLocation, :path => edit_service_point_location_path(service_point_location) } %>
        <%= render :partial => '/layouts/crud/delete', :locals => { :model => ServicePointLocation, :path => service_point_location, :msg => t("activerecord.models.service_point_location.one"), :txt => service_point_location.name } %> </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= render :partial => '/layouts/pagination', :locals => { :ivar => @service_point_locations } %>
<%= render :partial => '/layouts/crud/new', :locals => { :model => ServicePointLocation, :path => new_service_point_location_path, :txt => t("ag2_gest.service_point_locations.index.btn_new") } %>
<%= link_to t("ag2_gest.service_point_locations.index.back_to_management"), ag2_gest.service_point_management_path %>
