<%= form_for(@service_point_location) do |f| %>
  <% if @service_point_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_point_location.errors.count, "error") %> prohibited this service_point_location from being saved:</h2>

      <ul>
      <% @service_point_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
