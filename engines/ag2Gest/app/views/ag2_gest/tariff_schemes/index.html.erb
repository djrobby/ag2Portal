<h1>Listing tariff_schemes</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tariff_schemes.each do |tariff_scheme| %>
  <tr>
    <td><%= link_to 'Show', tariff_scheme %></td>
    <td><%= link_to 'Edit', edit_tariff_scheme_path(tariff_scheme) %></td>
    <td><%= link_to 'Destroy', tariff_scheme, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tariff scheme', new_tariff_scheme_path %>
