<%= form_for(@sale_offer_status) do |f| %>
  <% if @sale_offer_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale_offer_status.errors.count, "error") %> prohibited this sale_offer_status from being saved:</h2>

      <ul>
      <% @sale_offer_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
