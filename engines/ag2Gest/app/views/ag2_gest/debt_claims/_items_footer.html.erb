<!--
  Bill
  Invoice
  Client
  Original payday
  Status
  Current payday
  Original debt
  Original debt + current surcharge
  Current debt
-->
<tr class="totals">
  <td></td>
  <td></td>
  <td align="right"><%= t "activerecord.attributes.debt_claim.totals" %></td>
  <td></td>
  <td></td>
  <td></td>
  <td align="right"><%= number_with_precision(claim.debt, precision: 4, delimiter: I18n.locale == :es ? "." : ",") unless claim.debt.blank? %></td>
  <td align="right"><%= number_with_precision(claim.total, precision: 4, delimiter: I18n.locale == :es ? "." : ",") unless claim.total.blank? %></td>
  <td align="right"><%= number_with_precision(claim.current_total, precision: 4, delimiter: I18n.locale == :es ? "." : ",") unless claim.current_total.blank? %></td>
</tr>
