<script>
  $(document).ready(function() {
    $("#div_table th a, #div_table #pages a").on("click", function() {
      $.getScript(this.href);
      return false;
    });
  }); 
</script>

<table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th><%= sortable "code", t('activerecord.attributes.subscriber_annotation_class.name') %></th>
      <th><%= sortable "name", t('activerecord.attributes.subscriber_annotation_class.name') %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @subscriber_annotation_classes.each do |type| %>
    <tr>
      <td><%= link_to type.code, type %></td>
      <td><%= type.name unless type.name.blank? %></td>
      <td><%= render :partial => '/layouts/crud/edit', :locals => { :model => SubscriberAnnotationClass, :path => edit_subscriber_annotation_class_path(type) } %>
      <%= render :partial => '/layouts/crud/delete', :locals => { :model => SubscriberAnnotationClass, :path => type, :msg => t("activerecord.models.subscriber_annotation_class.one"), :txt => type.name } %> </td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= render :partial => '/layouts/pagination', :locals => { :ivar => @subscriber_annotation_classes } %>
<%= render :partial => '/layouts/crud/new', :locals => { :model => SubscriberAnnotationClass, :path => new_subscriber_annotation_class_path, :txt => t("ag2_gest.subscriber_annotation_classes.index.btn_new") } %>
<%= link_to t("ag2_gest.subscriber_annotation_classes.index.back_to_root"), ag2_gest.root_path %>
