<%= form_for(@subscriber_annotation_class) do |f| %>
  <% if @subscriber_annotation_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subscriber_annotation_class.errors.count, "error") %> prohibited this subscriber_annotation_class from being saved:</h2>

      <ul>
      <% @subscriber_annotation_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
