<%= form_for(@pre_reading) do |f| %>
  <% if @pre_reading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pre_reading.errors.count, "error") %> prohibited this pre_reading from being saved:</h2>

      <ul>
      <% @pre_reading.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
