<%= form_for(@complaint_status) do |f| %>
  <% if @complaint_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@complaint_status.errors.count, "error") %> prohibited this complaint_status from being saved:</h2>

      <ul>
      <% @complaint_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
