<%= form_for(@contract_template) do |f| %>
  <% if @contract_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contract_template.errors.count, "error") %> prohibited this contract_template from being saved:</h2>

      <ul>
      <% @contract_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
