<%= form_for(@billable_item) do |f| %>
  <% if @billable_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@billable_item.errors.count, "error") %> prohibited this billable_item from being saved:</h2>

      <ul>
      <% @billable_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
