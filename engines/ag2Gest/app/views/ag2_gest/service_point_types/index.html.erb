<h1>Listing service_point_types</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @service_point_types.each do |service_point_type| %>
  <tr>
    <td><%= link_to 'Show', service_point_type %></td>
    <td><%= link_to 'Edit', edit_service_point_type_path(service_point_type) %></td>
    <td><%= link_to 'Destroy', service_point_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Service point type', new_service_point_type_path %>
