<%= form_for(@service_point_type) do |f| %>
  <% if @service_point_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_point_type.errors.count, "error") %> prohibited this service_point_type from being saved:</h2>

      <ul>
      <% @service_point_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
