<script>
  $(document).ready(function() {
    $("#div_table th a, #div_table #pages a").on("click", function() {
      $.getScript(this.href);
      return false;
    });
  }); 
</script>

<table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th><%= sortable "id", t('activerecord.attributes.service_point_type.id') %></th>
      <th><%= sortable "name", t('activerecord.attributes.service_point_type.name') %></th>
    </tr>
  </thead>
  <tbody>
    <% @service_point_types.each do |service_point_type| %>
      <tr>
        <td><%= service_point_type.try(:id) %></td>
        <td><%= link_to service_point_type.try(:name), service_point_type_path(service_point_type) %></td>
        <td> <%= render :partial => '/layouts/crud/edit', :locals => { :model => ServicePointType, :path => edit_service_point_type_path(service_point_type) } %>
        <%= render :partial => '/layouts/crud/delete', :locals => { :model => ServicePointType, :path => service_point_type, :msg => t("activerecord.models.service_point_type.one"), :txt => service_point_type.name } %> </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= render :partial => '/layouts/pagination', :locals => { :ivar => @service_point_types } %>
<%= render :partial => '/layouts/crud/new', :locals => { :model => ServicePointType, :path => new_service_point_type_path, :txt => t("ag2_gest.service_point_types.index.btn_new") } %>
<%= link_to t("ag2_gest.service_point_types.index.back_to_management"), ag2_gest.service_point_management_path %>
