<%= form_for(@meter_model) do |f| %>
  <% if @meter_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meter_model.errors.count, "error") %> prohibited this meter_model from being saved:</h2>

      <ul>
      <% @meter_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
