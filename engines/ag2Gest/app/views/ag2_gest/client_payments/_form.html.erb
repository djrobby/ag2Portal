<%= form_for(@client_payment) do |f| %>
  <% if @client_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client_payment.errors.count, "error") %> prohibited this client_payment from being saved:</h2>

      <ul>
      <% @client_payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
