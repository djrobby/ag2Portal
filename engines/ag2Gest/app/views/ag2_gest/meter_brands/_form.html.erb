<%= form_for(@meter_brand) do |f| %>
  <% if @meter_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meter_brand.errors.count, "error") %> prohibited this meter_brand from being saved:</h2>

      <ul>
      <% @meter_brand.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
