<%= form_for(@complaint_document_type) do |f| %>
  <% if @complaint_document_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@complaint_document_type.errors.count, "error") %> prohibited this complaint_document_type from being saved:</h2>

      <ul>
      <% @complaint_document_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
