<h1>Listing reading_routes</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reading_routes.each do |reading_route| %>
  <tr>
    <td><%= link_to 'Show', reading_route %></td>
    <td><%= link_to 'Edit', edit_reading_route_path(reading_route) %></td>
    <td><%= link_to 'Destroy', reading_route, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reading route', new_reading_route_path %>
