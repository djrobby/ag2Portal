<%= form_for(@reading_route) do |f| %>
  <% if @reading_route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reading_route.errors.count, "error") %> prohibited this reading_route from being saved:</h2>

      <ul>
      <% @reading_route.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
