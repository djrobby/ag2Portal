<script>
  $(document).ready(function() {
    $("#div_table th a, #div_table #pages a").on("click", function() {
      $.getScript(this.href);
      return false;
    });
  });
</script>

<table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th><%= sortable "id", t('activerecord.attributes.caliber.caliber_id') %></th>
      <th><%= sortable "caliber", t('activerecord.attributes.caliber.caliber') %></th>
      <th><%= sortable "letter_id", t('activerecord.attributes.caliber.letter_id') %></th>
    </tr>
  </thead>
  <tbody>
    <% @calibers.each do |calib| %>
      <tr>
        <td><%= link_to calib.try(:id), caliber_path(calib) %></td>
        <td><%= calib.try(:caliber) %></td>
        <td><%= calib.try(:letter_id) %></td>
        <td>
          <%= link_to edit_caliber_path(calib), class: "btn btn-mini" do %>
            <i class="icon-edit"></i>
          <% end %>
          <%= link_to calib, class: "btn btn-mini", method: :delete, data: { confirm: 'Are you sure?' } do %>
            <i class="icon-trash"></i>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= render :partial => '/layouts/pagination', :locals => { :ivar => @calibers } %>
<%= render :partial => '/layouts/crud/new', :locals => { :model => Caliber, :path => new_caliber_path, :txt => t("ag2_gest.calibers.index.btn_new") } %>
<%= link_to t("ag2_gest.calibers.index.back_to_root"), ag2_gest.root_path %>
