<%= form_for(@caliber) do |f| %>
  <% if @caliber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caliber.errors.count, "error") %> prohibited this caliber from being saved:</h2>

      <ul>
      <% @caliber.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
