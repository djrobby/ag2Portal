<tr>
  <td></td>
  <td align="right"><%= t :"activerecord.attributes.invoice.quantity" %> | <%= t :"activerecord.attributes.invoice.subtotal" %></td>
  <td align="right"><%= number_with_precision(note.quantity, precision: 2) unless note.quantity.blank? %></td>
  <td></td>
  <td></td>
  <td></td>
  <td align="right"><%= number_with_precision(note.subtotal, precision: 2) unless note.subtotal.blank? %></td>
  <td></td>
</tr>
<tr>
  <td></td>
  <td align="right"><%= t :"activerecord.attributes.invoice.bonus" %> | <%= t :"activerecord.attributes.invoice.taxable" %> | <%= t :"activerecord.attributes.invoice.taxes" %></td>
  <td></td>
  <td></td>
  <td align="right"><%= number_with_precision(note.discount_pct, precision: 2) + "%" unless note.discount_pct.blank? %></td>
  <td align="right"><%= number_with_precision(note.bonus, precision: 2) unless note.bonus.blank? %></td>
  <td align="right"><%= number_with_precision(note.taxable, precision: 2) unless note.taxable.blank? %></td>
  <td align="right"><%= number_with_precision(note.taxes, precision: 2) unless note.taxes.blank? %></td>
</tr>
<tr class="totals">
  <td></td>
  <td align="right"><%= t :"activerecord.attributes.invoice.total" %></td>
  <td></td>
  <td></td>
  <td></td>
  <td></td>
  <td align="right"><%= number_with_precision(note.total, precision: 2, delimiter: I18n.locale == :es ? "." : ",") unless note.total.blank? %></td>
  <td></td>
</tr>
