<h1>Listing meter_owners</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @meter_owners.each do |meter_owner| %>
  <tr>
    <td><%= link_to 'Show', meter_owner %></td>
    <td><%= link_to 'Edit', edit_meter_owner_path(meter_owner) %></td>
    <td><%= link_to 'Destroy', meter_owner, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Meter owner', new_meter_owner_path %>
