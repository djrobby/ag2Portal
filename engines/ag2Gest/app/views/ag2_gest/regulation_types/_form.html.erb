<%= form_for(@regulation_type) do |f| %>
  <% if @regulation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@regulation_type.errors.count, "error") %> prohibited this regulation_type from being saved:</h2>

      <ul>
      <% @regulation_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
