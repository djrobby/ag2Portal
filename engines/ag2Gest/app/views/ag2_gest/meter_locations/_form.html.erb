<%= form_for(@meter_location) do |f| %>
  <% if @meter_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meter_location.errors.count, "error") %> prohibited this meter_location from being saved:</h2>

      <ul>
      <% @meter_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
