<%= form_for(@reading_type) do |f| %>
  <% if @reading_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reading_type.errors.count, "error") %> prohibited this reading_type from being saved:</h2>

      <ul>
      <% @reading_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
