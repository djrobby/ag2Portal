<%= form_for(@service_point) do |f| %>
  <% if @service_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_point.errors.count, "error") %> prohibited this service_point from being saved:</h2>

      <ul>
      <% @service_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
