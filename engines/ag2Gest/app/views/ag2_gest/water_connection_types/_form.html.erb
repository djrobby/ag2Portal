<%= form_for(@water_connection_type) do |f| %>
  <% if @water_connection_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@water_connection_type.errors.count, "error") %> prohibited this water_connection_type from being saved:</h2>

      <ul>
      <% @water_connection_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
