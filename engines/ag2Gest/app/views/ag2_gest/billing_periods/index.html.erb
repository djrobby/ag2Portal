<h1>Listing billing_periods</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @billing_periods.each do |billing_period| %>
  <tr>
    <td><%= link_to 'Show', billing_period %></td>
    <td><%= link_to 'Edit', edit_billing_period_path(billing_period) %></td>
    <td><%= link_to 'Destroy', billing_period, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Billing period', new_billing_period_path %>
