<%= form_for(@billing_period) do |f| %>
  <% if @billing_period.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@billing_period.errors.count, "error") %> prohibited this billing_period from being saved:</h2>

      <ul>
      <% @billing_period.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
