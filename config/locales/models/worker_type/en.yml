en:
  activerecord:
    models:
      worker_type:
        zero:  Worker types
        one:   Worker type
        few:   Worker types
        other: Worker types
    attributes:
      worker_type:
        description: "Description"
