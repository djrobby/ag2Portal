en:
  activerecord:
    models:
      timerecord_type:
        zero:  TimeRecord types
        one:   TimeRecord type
        few:   TimeRecord types
        other: TimeRecord types
    attributes:
      timerecord_type:
        name: "Type"
