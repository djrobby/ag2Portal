en:
  activerecord:
    models:
      timerecord_code:
        zero:  TimeRecord codes
        one:   TimeRecord code
        few:   TimeRecord codes
        other: TimeRecord codes
    attributes:
      timerecord_code:
        name: "Description"
