en:
  activerecord:
    models:
      work_order_type:
        zero:  Work order types
        one:   Work order type
        few:   Work order types
        other: Work order types
        check_for_work_orders: "Type cannot be destroyed while linked work orders exist."
    attributes:
      work_order_type:
        name: "Description"
