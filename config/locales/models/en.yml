en:
  yes_on: "Yes"
  no_off: "No"
  try_again: "Retry"
  should_try_again: "You should try again..."
  activerecord:
    errors:
      models:
        product:
          attributes:
            product_code:
              code_invalid: "invalid"
            image:
              attachment_invalid: "invalid (must be JPG, PNG, GIF or BMP)"
        product_family:
          attributes:
            family_code:
              code_invalid: "invalid (must be numeric)"
        entity:
          attributes:
            fiscal_id:
              fiscal_invalid: "invalid (must be alphanumeric without blanks or symbols)"
        supplier:
          attributes:
            supplier_code:
              code_invalid: "invalid"
        client:
          attributes:
            client_code:
              code_invalid: "invalid (must be numeric)"
        delivery_note:
          attributes:
            delivery_no:
              code_invalid: "invalid"
        offer_request:
          attributes:
            request_no:
              code_invalid: "invalid"
        offer:
          attributes:
            attachment:
              attachment_invalid: "invalid (must be JPG, PNG, GIF or BMP)"
        purchase_order:
          attributes:
            order_no:
              code_invalid: "invalid"
        project:
          attributes:
            project_code:
              code_invalid: "invalid"
        project_type:
          attributes:
            code:
              code_invalid: "invalid (must be alphabetic)"
        work_order:
          attributes:
            order_no:
              code_invalid: "invalid"
            completed_at:
              date_invalid: "Must be greater than or equal to Started at"
              date_invalid_full: "Completed at must be greater than or equal to Started at"
            closed_at:
              date_invalid: "Must be greater than or equal to Completed at"
              date_invalid_full: "Closed at must be greater than or equal to Completed at"
        charge_account:
          attributes:
            account_code:
              code_invalid: "invalid"
        charge_group:
          attributes:
            group_code:
              code_invalid: "invalid (must be numeric)"
        budget_period:
          attributes:
            period_code:
              code_invalid: "invalid (must be alphanumeric)"
        budget:
          attributes:
            budget_no:
              code_invalid: "invalid (must be alphanumeric)"
        budget_heading:
          attributes:
            heading_code:
              code_invalid: "invalid (must be numeric)"
        vehicle:
          attributes:
            registration:
              code_invalid: "invalid (must be alphanumeric without blanks)"
        accounting_group:
          attributes:
            code:
              code_invalid: "invalid (must be one numeric digit)"
        ticket:
          attributes:
            attachment:
              attachment_invalid: "invalid (must be JPG, PNG, GIF or BMP)"
        inventory_count:
          attributes:
            count_no:
              code_invalid: "invalid"
        supplier_payment:
          attributes:
            payment_no:
              code_invalid: "invalid"
        bank:
          attributes:
            code:
              code_invalid: "invalid"
        bank_office:
          attributes:
            code:
              code_invalid: "invalid"
        supplier_bank_account:
          iban_invalid: "Invalid IBAN."
          attributes:
            iban_dc:
              dc_invalid: "invalid (must be two numeric digits)"
            ccc_dc:
              dc_invalid: "invalid (must be two numeric digits)"
            account_no:
              code_invalid: "invalid (must be twelve numeric digits)"
        infrastructure:
          attributes:
            code:
              code_invalid: "invalid"
        meter:
          installation_error: "Error installing meter:"
          code_not_found: "%{var} not found."
          code_wrong: "%{var} is not correct."
          installed: "%{var} is already installed (unavailable)."
          available: "%{var} is available to be installed."
          attributes:
            meter_code:
              code_invalid: "inválido (debe ser alfanumérico)"
        meter_detail:
          attributes:
            installation_reading:
              reading_invalid: "invalid (must be null or integer greater than 0)"
        reading:
          attributes:
            reading_index:
              reading_invalid: "invalid (must be integer greater than 0)"
        formality:
          attributes:
            code:
              code_invalid: "invalid (must be alphanumeric)"
        client_bank_account:
          iban_invalid: "Invalid IBAN."
          attributes:
            iban_dc:
              dc_invalid: "invalid (must be two numeric digits)"
            ccc_dc:
              dc_invalid: "invalid (must be two numeric digits)"
            account_no:
              code_invalid: "invalid (must be twelve numeric digits)"
        company_bank_account:
          iban_invalid: "Invalid IBAN."
          attributes:
            iban_dc:
              dc_invalid: "invalid (must be two numeric digits)"
            ccc_dc:
              dc_invalid: "invalid (must be two numeric digits)"
            account_no:
              code_invalid: "invalid (must be twelve numeric digits)"
