es:
  activerecord:
    models:
      data_import_config: Configuración
    attributes:
      data_import_config:
        name: "Nombre"
        source: "Origen"
        target: "({Rails.root})Destino"
