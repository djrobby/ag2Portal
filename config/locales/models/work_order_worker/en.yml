en:
  activerecord:
    models:
      work_order_worker:
        zero:  Work order workers
        one:   Work order worker
        few:   Work order workers
        other: Work order workers
    attributes:
      work_order_worker:
        work_order: "Work order"
        work_order_id: "Work order"
        worker: "Worker"
        worker_id: "Worker"
        hours: "Hours"
        cost: "Cost"
        costs: "Costs"
