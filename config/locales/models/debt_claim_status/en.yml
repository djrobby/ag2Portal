en:
  activerecord:
    models:
      debt_claim_status:
        zero:  DC statuses
        one:   DC status
        few:   DC statuses
        other: DC statuses
        check_for_debt_claims: "Status cannot be destroyed while linked claims exist."
    attributes:
      debt_claim_status:
        name: "Description"
        action: "Action"
