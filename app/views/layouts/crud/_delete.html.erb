<% if can? :destroy, model %>
<%= link_to(path, :data => { :confirm => I18n.t("are_you_sure_to_delete") + "#{msg}? (#{txt})" }, :method => :delete, :class => 'btn btn-mini') do %> <i class="icon-trash"></i> <% end %>
<% end %>
