<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "aGestiona2 | ag2Portal" %></title>
    <meta name="description" content="aGestiona2 ag2Portal">
    <meta name="author" content="Nestor Rodriguez (SPSI) para AYG">
    <%= favicon_link_tag "/favicon.ico?1" %>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
    <script language="javascript" type="text/javascript">
      <!--
      var defaultPage;
      var resolution;

      window.onload = function() {
        // Min resolution for this site: 1024x768
        // LowRes resolution for this site: 1280x900 or less
        // Default resolution for this site: 1280x900 or more
        // Default site home page: home#index
        // LowRes site home page: lowres#index
        defaultPage = RedirectUponScreenResolution(1280, 900);
        if (defaultPage == "icon") {
          resolution = "<% session[:resolution] = 'LD' %>";
        } else {
          if (defaultPage == "default") {
            resolution = "<% session[:resolution] = 'HD' %>";
          } else {
            resolution = "<% session[:resolution] = 'SD' %>";
          }
        }
      }
      // -->
    </script>
  </head>
  <body>
    <!-- Header NavBar -->
    <header class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container">
          <%= render '/layouts/navigation' %>
        </div>
      </nav>
    </header>
    <!-- Main container -->
    <div id="main" role="main">
      <div class="container">
        <div class="row">
          <div class="span12">
            <%= render '/layouts/messages' %>
            <%= yield %>
          </div>
        </div>
      </div>
    </div><!-- /#main -->
    <!-- Footer NavBar -->
    <footer class="navbar navbar-fixed-bottom">
      <div class="navbar-inner">
        <div class="container">
          <%= render '/layouts/footernav' %>
        </div>
      </div>
    </footer>
  </body>
</html>
